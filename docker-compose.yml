services:
  gunicorn: &app_c
    build: .
    command: "gunicorn --bind 0.0.0.0:8000 --workers 3 stagingserver.asgi:application -k uvicorn.workers.UvicornWorker"
    volumes:
      - "./db.sqlite3:/STAGING_APP/db.sqlite3:rw"
      - "./STAGING_DIR:/STAGING_DIR"
      - "/var/run/docker.sock:/var/run/docker.sock"
      - "./staging_logs.out:/STAGING_APP/staging_logs.out"
      - "/etc/nginx/sites-enabled:/etc/nginx/sites-enabled"
      - "/etc/nginx/sites-available:/etc/nginx/sites-available"
    environment:
      - DEBUG=0
    depends_on:
      - redis
  redis:
    image: redis:alpine
  celery:
    build: .
    command: "celery -A stagingserver worker -l info"
    volumes:
      - "./db.sqlite3:/STAGING_APP/db.sqlite3:rw"
      - "./STAGING_DIR:/STAGING_DIR"
      - "/var/run/docker.sock:/var/run/docker.sock"
      - "./celery_logs.out:/STAGING_APP/celery_logs.out"
      - "/etc/nginx/sites-enabled:/etc/nginx/sites-enabled"
      - "/etc/nginx/sites-available:/etc/nginx/sites-available"
    depends_on:
      - gunicorn
      - redis
  nginx:
    image: nginx
    volumes:
      - './docker/dev.conf:/etc/nginx/conf.d/default.conf:ro'
      - './assets:/var/www/public/:ro'
    ports:
      - '127.0.0.1:8000:8000'
    depends_on:
      - gunicorn
  socat:
    image: alpine/socat
    command: "TCP-LISTEN:2375,reuseaddr,fork UNIX-CONNECT:/var/run/docker.sock"
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock"
